# 文件	加载顺序	作用
# bootstrap.yaml	最先加载	配置 Nacos 连接、应用名、profile
# application.yaml	后加载	配置服务特有参数（端口、端点、白名单）
spring:
  application:
    name: gateway-service
  config:
    import:
      - optional:nacos:shared-jwt.yaml
  cloud:
    nacos:
      server-addr: ${DOJ_NACOS_SERVER_ADDR:nacos:8848}
